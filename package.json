{
  "name": "these-spm",
  "version": "1.0.0",
  "private": true,
  "description": "Thèse DBA Serge Patrick MINANG — site personnel de recherche (dépôt autonome)",
  "scripts": {
    "test": "node tests/site.test.mjs",
    "serve": "python3 -m http.server 5173 --bind 0.0.0.0",
    "check": "npm test"
  },
  "engines": {
    "node": ">=18"
  }
}
